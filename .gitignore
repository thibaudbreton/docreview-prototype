# macOS
.DS_Store

# Local tooling config
.claude/

# Real RFP capture data — actual client tender content (requirement text,
# source files). Never publish to the public repo. Regenerate data.js locally
# via import_capture.py; see its own docstring.
data.js
Turnkey RFP Example/

